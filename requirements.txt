# Core dependencies
langchain-core>=0.3.81
langchain-openai>=0.3.23

# Data processing
pandas>=2.3.0
numpy>=1.24.0

# HTTP & APIs
aiohttp>=3.9.0
requests>=2.32.4

# Hyperliquid trading + EIP-712 signing
# NOTE: install "hyperliquid-python-sdk", NOT "hyperliquid" - a different package that shares the
# same import name and breaks `from hyperliquid.exchange import Exchange`
hyperliquid-python-sdk>=0.20.0
eth-account>=0.13.0

# Configuration
python-dotenv>=1.0.0

# UI & Output
rich>=14.0.0
typer>=0.21.0

# Development (optional)
ruff>=0.15
pytest>=8.0
pytest-asyncio>=0.23.0
